# Server
server.port=8083

# Database
spring.datasource.url=jdbc:mysql://localhost:3306/skilled_worker_booking?useSSL=false&serverTimezone=Asia/Colombo&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=2002Chamindu
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Colombo
spring.jpa.open-in-view=false

# Timezone
spring.jackson.time-zone=Asia/Colombo

# JWT
app.jwt.secret=ITP_SE_01_SkilledWorkerBookingSystem_SuperSecretKey_2026_ChangeInProduction
app.jwt.expiration-ms=86400000

# Google OAuth
google.client-id=737302176157-8b23ul5niaom24ur5pukhcllt89gmfir.apps.googleusercontent.com

# CORS
app.cors.allowed-origins=http://localhost:5173,http://localhost:5174,http://localhost:5175,http://localhost:5176,http://localhost:5177,http://localhost:5178,http://localhost:5179,http://localhost:5180

# File Upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Mail (configure SMTP for real emails; simulated for now)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
# FIXME: Replace with your actual email address
spring.mail.username=your-email@gmail.com
# FIXME: Replace with your actual app password (if using Gmail, generate an App Password)
spring.mail.password=your-app-password
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Disable Mail Health Check to prevent startup/runtime errors due to invalid credentials
management.health.mail.enabled=false

# Logging
logging.level.com.itp=DEBUG
logging.level.org.springframework.security=WARN
